/**
 * ============================================================================
 * LOGGING
 * ============================================================================
 * 
 * Index of exports:
 * - LogContext - Log context for structured logging
 */

/**
 * Log context for structured logging
 */
export interface LogContext {
	context: string;
	path: string;
}